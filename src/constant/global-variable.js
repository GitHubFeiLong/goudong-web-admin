/* 全局变量*/

const TABLE_HEIGHT = '500'
// 日期时间格式化字符串
const DATE_TIME_FORMATTER = 'YYYY-MM-DD HH:mm:ss'

// 分页组件的页大小选择
const PAGE_SIZES = [100, 200, 300, 400]

export default {
  TABLE_HEIGHT,
  DATE_TIME_FORMATTER,
  PAGE_SIZES
}
